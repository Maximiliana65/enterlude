# 制作日誌

Claudeとこのプロジェクトを作っていく過程のメモです。
新しい会話セッションを始めるとき、Claudeはこれまでの記憶を持たないので、
このファイルを一緒に見せてもらえると、すぐに状況を思い出せます。

（次回以降の書き方の例: 日付 → その日話したこと・決めたこと・作ったもの・次回への申し送り、の順で追記していくとやりやすいです）

---

## 2026-07-16　設計スタート〜フェーズ1実装

### 話したこと・決めたこと
- そもそもの困りごと: AIとの会話でうっかりEnterや再試行ボタンを押して誤送信してしまう
- GitHubの既存拡張機能をそのまま入れるのは不安 → 自作する方針に
- Enterキーの挙動は「常に改行、送信は明示的な解除操作をした時だけ」に決定
- 送信ボタン・再試行ボタンの誤クリックも同じ仕組みでブロックすることに（Enterだけ対策しても根本解決にならないため）
- ロック解除は🔒アイコンのクリック＋ショートカット(Ctrl+Shift+U)の両方を用意
- 解除は「次の1回だけ」有効。送信したら自動でまた鍵がかかる（毎回慎重に、という方針）
- 対応サイトはまずClaude.aiのみ。動作確認後にChatGPT・Geminiへ拡大
- お楽しみ機能（一言コメント表示）は初期状態OFF。送信した瞬間に表示。コメントは`fun/comments.js`にただの配列として書くので、後から自分で気軽に追記できる形にした
- 保守性を重視し、「コア（共通ロジック）／サイトアダプター／お楽しみプラグイン」の3層構造に設計
- 将来的にGitHubで公開したいので、`_locales`を使った多言語対応の土台も最初から用意（今回は日本語＋英語）
- Gitはまずプライベートリポジトリで始める方針

### 作ったもの
- フェーズ1一式（`manifest.json`、`core/`、`adapters/claude.js`、`fun/`、`popup/`、アイコン、`_locales`）
- README / ROADMAP / CHANGELOG / この日誌
- 設計書（`docs/`）

### 次回への申し送り
- **まだ実際のClaude.aiの画面で動作確認していない。** `adapters/claude.js`のセレクタが実際のDOMと合っているか要チェック
- 動作確認しながら、バッジの表示位置やロック解除の感触を調整する可能性あり
- Git init〜GitHubへのプライベートリポジトリ作成はこれから

---

## 2026-07-17　実際に使ってみてのフィードバック反映

### 実際に使ってみての感想(良かった点)
- ロック中の表示が控えめで邪魔にならない
- 「うっかり」がかなり予防できている実感あり
- 再試行ボタンのロックも好評

### 直した点
- 🔒アイコンをクリックのたびにロック⇔解除が切り替わる「トグルボタン」に変更
  （実際に使うとEscでのキャンセルが多く、ショートカットはあまり使わなかったとのフィードバックから）
- お楽しみコメントの背景色を、バッジと同じ明るい配色に統一
  （真っ黒だとライトモードで浮いて見えるとの指摘から）
- 設定画面(ポップアップ)に「ツールバーにピン留め」のヒントを追加
  （設定の場所が分かりにくいとの指摘。Chromeは拡張機能を既定でパズルピースの中に隠すため）

### 今後の検討事項として記録
- 色覚多様性・目の疲れにくさへの配慮を、今後の見た目調整の際に意識する
- ダークモードでの見え方も継続してチェックする

### セキュリティチェックリストの確認結果
- ユーザーが用意したチェックリスト(権限/外部通信/HTML注入/対象サイト/個人情報/ストア審査)を
  実際にコードを検索して確認 → 現時点では全項目クリア
- 外部通信なし、innerHTML未使用、権限は`storage`のみ、対象は`claude.ai`のみ

---

## 2026-07-18　フェーズ2実装とバージョン管理の導入

### やったこと
- ChatGPT・Gemini向けのアダプター(`adapters/chatgpt.js`, `adapters/gemini.js`)を追加
- アダプター間で使い回せる共通ヘルパー(`core/dom-utils.js`)を新設し、Claude用アダプターもそちらに揃えた
- 各サイトの入力欄・送信ボタンの想定セレクタは、Web検索で見つけた類似の公開スクリプト(Tampermonkey用)を
  参考に、一般的なパターン(`contenteditable`、`aria-label`に"send"/"送信"を含むボタン等)を採用
  （コードはそのまま流用せず、判定の考え方だけ参考にして自前で実装）
- `manifest.json` にChatGPT(`chatgpt.com`/`chat.openai.com`)・Gemini(`gemini.google.com`)の
  `host_permissions`と`content_scripts`を追加
- セマンティックバージョニング(`MAJOR.MINOR.PATCH`)を導入。`manifest.json`のバージョンを
  `0.1.0` → `0.2.0`に更新し、Gitタグ`v0.1.0`(フェーズ1完了時点)・`v0.2.0`(今回)を付与

### 次回への申し送り
- **ChatGPT・Geminiともに、まだ実際の画面での動作確認をしていない。** セレクタが合っているか要チェック
- 動作確認の結果次第で、`adapters/chatgpt.js`・`adapters/gemini.js`のセレクタ調整が必要になる可能性が高い
- 今後、区切りの良いタイミングでバージョンを上げてタグを打つ運用を続ける（判断はClaudeが提案する）

---

## 2026-07-18　ChatGPTでEnter送信がブロックされない不具合を調査・修正

### 報告内容
- ChatGPTで動作確認: 送信ボタン・再試行ボタンのロックは効いたが、Enterキーでは送信されてしまった
- ユーザー自身がChatGPTと一緒にF12のElementsパネルで調査し、入力欄が
  `<div id="prompt-textarea" class="ProseMirror" contenteditable="true" role="textbox">`
  であることを特定して報告してくれた（とても助かる調査だった）

### 原因(推定)
- 入力欄自体はセレクタに含まれており、検出はできていたはず
- 有力な原因は「タイミングの競争」: 拡張機能のコードが`document_idle`(ページ読み込み後半)で
  動き始めていたため、ChatGPT自身がEnterキーを処理する仕組みの方が一瞬早く反応してしまい、
  横取りする前に送信処理が進んでしまっていたと考えられる
- 送信ボタンのクリックの方はブロックできていたのは、おそらく別の仕組み(要素固有の処理)で
  実装されていて、タイミングの影響を受けにくかったため

### 対処
- `manifest.json`の`run_at`を`document_start`に変更(サイト自身のJSが動く前に監視を開始)
- 監視対象を`document`から一段外側の`window`に変更(より確実に先回りできるように)
- `document_start`では`<body>`がまだ無いことがあるため、バッジ表示だけ`DOMContentLoaded`まで
  遅延させるよう調整(キー・クリックの監視自体は即座に開始)
- バージョンを`0.2.0` → `0.2.1`(バグ修正のためPATCH)

### 次回への申し送り
- **この修正がChatGPTで実際に直っているか、再度確認が必要**
- ロック中にEnterで挿入される改行が、ProseMirror上で正しく表示されるかも要確認
  (もし改行が消えたり変な挙動になったら、挿入方法を再検討する)
- Geminiはまだ未確認のまま

### ちょっとしたつまずき
- フォルダ一括作成のコマンド(`mkdir -p .../{ja,en}`)がうまく展開されず、
  `_locales/ja` と `_locales/en` の中身(`messages.json`)が空のまま先に進んでしまっていた
- 最終チェックで発覚し、2つのファイルを作り直して解決済み
- 教訓: フォルダを複数まとめて作るコマンドを使ったときは、後でちゃんと中身ができているか一度見返すと安心

---

## 2026-07-18　ChatGPTのEnterガードをMAIN worldへ分離（v0.2.2）

### 再発確認
- `document_start` と `window` のcapture監視に変えた後も、実機ではChatGPTだけEnter送信を止められなかった
- 送信ボタンのロックは引き続き有効だったため、入力欄の判定ではなく、キーイベントの処理順が原因と判断した

### 設計変更
- **送信を止める処理だけ**を、ChatGPTページ本体と同じ `MAIN world` で実行する `core/chatgpt-page-guard.js` に分離した
- バッジ、ロック状態、設定、お楽しみ機能は従来どおり隔離されたcontent scriptの共通コアが担当する
- 両者の状態共有には、`<html>` の `data-send-guard-unlocked` 属性と小さなイベント通知を使う

### ねらい
- ChatGPT側のwindowイベントリスナーより先に、同じ実行コンテキストでEnterを止められる
- ChatGPT固有のイベント順序の問題を、Claude/Geminiの共通処理に持ち込まない
- 今後ほかのサービスで同じ問題が出た場合も、同じ構成の専用ページガードを追加できる

### 確認
- JavaScriptの構文チェックとmanifestの検証を実施
- 簡易イベントテストで、ロック中のEnterが送信されず改行処理になること、解除中のEnterが一度だけ通って再ロックされることを確認
- 実際のChatGPT画面での最終確認は、拡張機能を再読み込みしてから行う

---

## 2026-07-18　ChatGPTで空白文字が入る問題の修正（v0.2.3）

- MAIN worldガードがProseMirrorに直接改行を挿入すると、ChatGPT側で改行ではなく空白文字のように扱われることがあった
- 直接DOMを変更せず、ChatGPTで通常の改行として使われるShift+Enterのキー処理を呼ぶ方式へ変更
- これにより、送信を止める責務は拡張機能、改行を作る責務はChatGPT本体と明確に分離した

---

## 2026-07-18　Geminiの解除済みEnter送信を修正（v0.2.4）

### 発生していたこと
- Geminiでは、ロックを解除してEnterを押しても送信されず、お楽しみ表示だけが出て再ロックされていた
- 一方で送信ボタンのクリックは正常だった

### 設計変更
- Geminiのキー監視を共通content scriptから、ページ本体と同じMAIN worldの専用ガードへ移動
- Enterを許可した直後には再ロックしない。Geminiが入力欄を空にしたことを監視して、送信開始を確認してから再ロックする

### 効果
- EnterイベントをGemini自身の処理にそのまま渡せる
- 送信されなかった場合はロック解除状態を保つため、お楽しみ表示だけが出る誤判定を防げる

---

## 2026-07-19　Codex主導での修正内容をレビュー

### 経緯
- ChatGPT・GeminiのEnter対応について、Codexと一緒にF12で調査・修正してもらい、
  実機で動作確認まで完了した状態のファイル一式を共有してもらった
- Claudeの手元に残っていた前回のプロジェクト一式にファイルをマージし、全体を検証

### 確認した内容
- manifest.json・全JSファイルの構文チェック → 問題なし
- innerHTML・eval・外部通信の有無 → 新規ファイルにも該当なし(セキュリティ方針を維持)
- MAIN world / ISOLATED world 間の状態同期ロジック(`data-send-guard-unlocked`属性 + カスタムイベント)
  → 無限ループなし、バッジクリック・Enterキー・ショートカットのどこから状態を変えても正しく同期される
- ChatGPT版の「Shift+Enterを模したイベント発火」処理 → 自分自身の処理に再度引っかからないよう
  `shiftKey`チェックで正しく除外されていることを確認
- Gemini版の「入力欄が実際に空になるまで待って再ロック」処理 → MutationObserverの後始末(5秒でタイムアウト)も含めて問題なし

### 直した点(ドキュメントのみ)
- README.md / ROADMAP.md のステータス表記が「フェーズ1のみ」のまま古くなっていたため、
  実態(Claude・ChatGPT・Gemini全て動作確認済み)に合わせて更新

### 所感
- MAIN world世界での専用ガードという設計は、ISOLATED worldとページ本体の実行順序の競合という
  根本原因に対する、正攻法の解決策になっている

---

## 2026-07-19　お楽しみコメント拡充・季節限定機能・Edge対応の相談（v0.3.0）

### Geminiについて
- 動作確認をしたかったが状況が再現できず、持ち越しに。ROADMAP/READMEのステータス表記を
  「Claude・ChatGPTは確認済み、Geminiは持ち越し中」という正直な状態に戻した

### お楽しみコメント
- Claude・ChatGPT・Gemini(?)みんなで考えたコメントを大量に追加(`fun/comments.js`)
- ユーザーから「季節限定コメントは難しいか」と質問があり、仕組み自体は簡単なことを説明した上で実装
  - コメントに`season: {from, to}`(月-日)を追加するだけで、その期間だけ登場するようにできる
  - 年をまたぐ期間(年末年始など)にも対応できるようロジックをテスト済み
  - クリスマス・お正月・ハロウィンをサンプルとして追加(文言は仮なので今後調整可)

### Edge対応について
- 「どのフェーズでやるか」と質問があったので回答:
  EdgeはChromiumベースでManifest V3もそのまま動くため、コード変更は基本的に不要
  「新しく作る」というより「同じものを別のストアにも出す」だけなので、フェーズ4(公開準備)に位置付けた
  今すぐ`edge://extensions`の開発者モードで試すこと自体は可能、と伝えた

### Grok・Copilot対応
- ユーザーの判断で見送りに。ROADMAPの将来検討事項から取り消し線付きで記録

### 次回への申し送り
- Geminiの動作確認、再現できるタイミングがあれば実施
- 季節限定コメントの文言(クリスマス・お正月・ハロウィン)は仮なので、気が向いたら調整

---

## 2026-07-19　お楽しみコメントの英語版を追加(v0.4.0)

### やったこと
- ユーザーが用意した英語版コメント一式を追加
- `fun/comments.js`(単一配列)を`fun/comments.ja.js`/`fun/comments.en.js`(言語ごとの配列)に分割
- `fun-engine.js`に`chrome.i18n.getUILanguage()`でブラウザの表示言語を見て、
  日本語なら`ja`、それ以外なら`en`のコメントを使う`_pickCommentsList()`を追加
  (片方しか用意されていない場合は、あるほうにフォールバックする)
- manifest.jsonの読み込みファイル一覧を更新
- Node.jsでモックした簡易テストで、`en-US`→英語(101件)、`ja`→日本語(101件)と、
  意図通りに切り替わることを確認

### 次回への申し送り
- 英語コメントは自分でチェックしていないため、実際にブラウザの言語を英語にして
  表示を確認したい場合は教えてください(Chromeの設定言語を変えるか、`chrome://settings/languages`で確認可能)

---

## 2026-07-19　Geminiのやり直すボタン修正を反映、コメントの見た目をカプセル型に、特別演出を追加（v0.4.1 → v0.5.0）

### Geminiの修正を反映
- Codexと一緒に調査・修正した「やり直すボタンがロック中も効いてしまう」不具合の修正ファイルを受け取り、
  手元のプロジェクトに統合。整合性・構文・セキュリティを再検証し、問題なしを確認
- pointerdown/mousedown段階からの遮断、Enter/Spaceキーでのボタン操作への対応など、
  ロジックを1つずつ追って矛盾がないことを確認(特に「解除中にキー操作された場合、キー処理側では
  何もしないが、その後ブラウザが発生させるclickイベントで正しく再ロックされる」という設計を確認)

### コメントの見た目をカプセル型に
- Visualizerで先にプレビューを見てもらってから、`core/lock-ui.css`の吹き出しを
  `border-radius: 999px`(バッジと同じ完全な角丸)に変更

### 特別な演出を追加
- Claude・ChatGPT・Geminiみんなで考案した演出をユーザーが採用:
  初回送信・10/50/100回目の節目・深夜0-4時のコメント
- `fun/special-comments.js`を新設(日英対応)。優先順位は「初回 > 節目 > 深夜 > 通常のランダム」
- 累計送信回数を数えるため、`core/settings-store.js`に`increment()`を追加
  (`chrome.storage.sync`に保存されるので、複数の対応サイトをまたいで通算でカウントされる)
- Node.jsでのモックテストで、各節目・深夜判定・優先順位の重なりを確認済み

### 次回への申し送り
- 実機で、実際に10回・50回と送信して節目コメントが出るか確認したい場合は教えてください
  (待つのが大変なら、`chrome.storage.sync.set({totalSentCount: 9})`のように値を直接書き換えて
  次の1回で10回目を再現する、という近道も可能です)

---

## 2026-07-19　特別演出(初回/節目/深夜)を撤回(v0.5.1)

### 経緯
- 実際に動作確認したところ、拡張機能を更新した直後に「1回目」の判定になり「はじめまして」が
  出てしまった(カウントの扱いが更新のたびにズレうることが判明)
- ユーザーから「カウント系は深追いすると沼りそう」との判断があり、10/50/100回目の節目演出も含めて撤回することに
- 深夜0-4時の「夜更かしさん?」も、ChatGPTから「夜更かしと言われたくない」という意見が出たとのことで撤回
- カプセル型への変更(見た目)は好評だったため、そのまま維持

### やったこと
- `fun/special-comments.js`を削除
- `fun-engine.js`を、特別演出を追加する前(通常のランダムコメントのみ)の状態に戻した
- `core/settings-store.js`から`totalSentCount`・`increment()`を削除
- `manifest.json`から`fun/special-comments.js`の読み込みを削除
- バージョンを`0.5.0` → `0.5.1`(取り下げのためPATCH)

### 所感
- 実装して実際に触ってみたことで「カウント管理は複雑になりやすい」と早い段階で気づけたのは収穫
- 気に入らなければ気軽に撤回できるのも、コア(送信ロック本体)とお楽しみ機能を分離して設計しておいた効果

### 次回への申し送り
- フェーズ4(公開準備)に進む。ユーザーは初めての公開作業なので、ステップバイステップで進める

---

## 2026-07-19　設計書のファイル名変更、フェーズ4着手(ライセンス・バッジ位置調整)

### 設計書のファイル名
- 日本語ファイル名(`設計書_AI誤送信防止拡張機能.md`)が文字化けするとの報告を受け、
  `docs/DESIGN.md`にリネーム(git mvで履歴を保持)。READMEにもリンクを追加

### フェーズ4開始: ライセンス
- MITライセンスを採用することに決定(改造・再配布・商用利用も自由、著作権表示のみ残す)
- LICENSEファイル作成は、著作権表示に入れる名前を確認してから完了させる

### バッジの位置調整(v0.5.2)
- ユーザーがChatGPT/Geminiを画面分割して使った際、バッジが送信ボタンと重なることを発見
  (Claudeでは発生せず。おそらくサイトごとの入力欄の余白の違いが原因)
- ChatGPTから「1200px未満でバッジを上にずらす」という簡易対応の提案があり、採用
  - 実装難易度は低い(CSSのメディアクエリ1つ)と判断し、その場で対応
  - 「送信ボタンの実際の位置を自動検出する」というより高度な対応はROADMAPの
    将来検討事項に記録し、深追いしないことに

### 次回への申し送り
- LICENSEファイルの著作権者名を確認して完成させる
- フェーズ4の残りタスク: READMEの見直し・GitHubプライベートリポジトリ作成・Edgeでの動作確認

---

## 2026-07-19　LICENSE作成、バッジ位置をサイトごとに調整可能に(v0.5.3)

### LICENSE
- 著作権表示の名前を確認し、MITライセンスの`LICENSE`ファイルを作成(Copyright (c) 2026 Maximiliana65)
- READMEのライセンス表記も更新

### バッジ位置のさらなる調整
- v0.5.2で「画面幅1200px以下で70px上げる」対応をしたが、ユーザーが実際に幅を細かく
  変えて検証したところ、以下が判明:
  - Geminiは画面のちょうど半分くらいの幅で、バッジが送信ボタンと完全に重なってしまう
  - Claudeも、狭くすると以前ほど快適ではなく、わずかに重なる(ただしユーザー自身は
    「検証の不備かもしれない」とも述べていた)
- 各サイトのDOM構造の違いが原因のため、CSSで一律に調整するには限界があると判断
- アダプターに`siteId`(`claude`/`chatgpt`/`gemini`)を追加し、`content-main.js`が
  `<html>`要素に`data-send-guard-site`属性として反映する仕組みを新設
  - これにより`core/lock-ui.css`側で、サイトごとに個別のスタイル調整が可能に
- 具体的な調整値:
  - 全サイト共通: 狭い画面(1200px以下)でのずらし幅を70px→92pxに増加
  - Geminiのみ: 狭い画面でさらに強く(130px)ずらす追加ルールを設定
- 正確なピクセル値は実機でしか検証できないため、あくまで現時点での見立て。
  引き続きユーザーの実機確認をもとに微調整していく方針

### 所感
- 「サイトごとに調整できる仕組み」自体は、今後どのサイトで似た問題が起きても
  同じやり方(siteId追加→CSSにルール追加)で対応できるため、汎用性のある改善になった

### 次回への申し送り
- Gemini・Claude・ChatGPTそれぞれで、狭い画面幅での重なりを再確認したい
- フェーズ4の残りタスク: READMEの見直し・GitHubプライベートリポジトリ作成・Edgeでの動作確認

---

## 2026-07-19〜20　フェーズ4: README整備・アイコンの使い分け決定

### Edge動作確認
- Chrome/Edge、通常・シークレット両モード、3サイトすべてで動作確認完了との報告
- ROADMAPを更新(Edgeアドオンストアへの登録はGitHub公開後に検討、として先送り)

### README刷新
- スクリーンショット(ロック中/解除中)をユーザーが用意、`docs/images/`に配置してREADMEに埋め込み
- 「使い方の流れ」の図解をVisualizerで作成し、SVGファイルとしても保存して埋め込み
  (入力→Enter/送信→ロック中なら改行・解除中なら送信して自動再ロック、という一連の流れ)
- ステータス表記を最新化、Edgeのインストール手順を追加、バージョン/ライセンスバッジを追加

### アイコンの使い分けを決定
- ユーザーが新アイコン案を2つ提示:
  1. 花の中心で星が輝くデザイン(「大切なメッセージを守る」イメージ、ブランドロゴ寄り)
  2. 盾+鍵のデザイン(一度見送ったが改めて共有)
- どちらも16pxに縮小して実際の見え方を確認した上でフィードバック:
  細かいデザインは16pxでは輪郭がにじみやすく、今の鍵アイコン(シンプルな分、小さくても伝わりやすい)
  の方がツールバー用としては強いと助言
- 結論: **使い分け**を採用
  - ツールバー(`icons/`)は現行の鍵アイコンのまま変更なし
  - README冒頭やブランド表現には、花+星のデザインを`docs/images/logo.png`として採用
- 盾+鍵のデザインは今回は不採用(ユーザーの希望で花+星を選択)。まだ変える余地はある前提で先に進む

### 次回への申し送り
- フェーズ4の残り: GitHubにプライベートリポジトリを作成してアップロード

---

## 2026-07-20　GitHubへの初公開🎉

### やったこと
- `git bundle`でコミット履歴・タグ(v0.1.0〜v0.5.3)を丸ごと持ち出せる形にして、
  ユーザー自身の手元のパソコンで復元・公開してもらう手順を案内
- GitHubにプライベートリポジトリ`send-guard`を新規作成
- `git clone`(バンドルから復元)→`git remote add origin`→`git push origin main --tags`
  の手順で、コミット履歴・タグを含めて正常にGitHubへ反映完了

### 所感
- 設計相談から始まり、実装・デバッグ・多言語対応・お楽しみ機能の実装と撤回・
  ライセンス選定・README整備を経て、ついに最初のGitHub公開まで到達
- 開発の全過程がDEVLOGとコミット履歴の両方に残っているので、今後読み返しても
  経緯を追いやすい状態になっている

### 次回への申し送り
- フェーズ4の残り: `_locales/en`の最終チェック、Edgeアドオンストアへの登録(公開判断は任意)
- 公開範囲(プライベート→パブリック)を変更するタイミングは、ユーザーの判断で今後相談

---

## 2026-07-20　最終確認完了、DESIGN.mdにChatGPTのレビューを反映

### 確認完了
- 英語表示（`_locales/en`）を実機で確認、問題なし
- Geminiで長時間の会話・送信を試したが、異常なし（狭い画面幅での重なりも解消済みを確認）
- README更新分のバンドル同期、無事完了

### DESIGN.mdのレビュー反映
- ChatGPTからDESIGN.mdのレビューをもらい、内容を検討
- 主な指摘: 設計書の内容(「第一弾: Claudeのみ」等)が、実際の実装状況(3サイト対応済み)と
  ズレてきている。対応として「内容を書き換える」のではなく「初期設計時点の記録として残し、
  現状との差分はDEVLOG/CHANGELOGを参照するよう注記する」提案があり、採用
- 具体的な変更:
  - 冒頭に「初期設計(2026年7月16日時点)の記録である」旨の注記を追加、DEVLOG/CHANGELOGへの
    リンクを追加
  - 「非エンジニアが開発・保守することを前提に」を「保守しやすさ・拡張しやすさを重視した
    設計とする」に言い換え(誰が作るかでなく設計方針に焦点を当てる表現に)
- それ以外の内容(アーキテクチャ図、機能要件など)はあえて書き換えず、当時の考えをそのまま保持

### 次回への申し送り
- フェーズ4の残りタスクはほぼ完了。Edgeアドオンストアへの登録は任意で、今後の判断次第

---

## 2026-07-20　GitHubをPublicに切り替え、正式公開🎉🎉

### やったこと
- プライベートリポジトリ`send-guard`を、GitHubの設定からPublicに切り替え
- 公開後、実際のページを確認。ロゴ・スクリーンショット・図解・Limitations・ライセンスバッジなど
  すべて意図通り表示されていることを確認
- README内の自己参照リンク(「日本語」が自分自身を指していた)という小さな不具合を発見・修正
- README/README.en.md/ROADMAPのステータス表記を「公開準備中」から「公開済み」に更新
- ROADMAPのフェーズ1〜4、すべて完了として整理

### 感想
- 「うっかりEnterを押してしまう」という個人的な困りごとの相談から始まり、設計・実装・
  複数のAIとの共同デバッグ・お楽しみ機能の実装と撤回・セキュリティチェック・多言語対応・
  README整備を経て、初めてのGitHub公開まで到達
- プライベートで始めてから最終確認をしてパブリックに切り替える、という段階を踏んだ進め方が、
  安心して公開まで進める助けになった

### 今後
- Edgeアドオンストアへの登録は任意。気が向いたタイミングで相談
- それ以外は、ROADMAPの「将来的な検討事項」を参照しつつ、必要に応じて機能追加していく

---

## 2026-07-20　Geminiサイドパネルへの対応を見送り、READMEに対応範囲を明記

### 相談内容
- Chromeの「デザイン」設定から開けるGeminiサイドパネル(ミニGemini)が、拡張機能でロックできないと判明
- Gemini・ChatGPT両方から「深追いせず、非対応と明記するのがよい」とのアドバイスがあり、
  Claudeにも意見を求められた

### 調査した内容
- Web検索で確認したところ、Chromeの「Geminiサイドパネル」はGmail・Keep・Drive・YouTubeの
  内容を横断的に読み取れる等、通常のgemini.google.comのWebページとは異なる、
  **Chromeブラウザ自体に内蔵されたネイティブ機能**である可能性が高いことが分かった
- コンテンツスクリプトは「該当URLへの通常の読み込み」を検知して差し込む仕組みのため、
  サイドパネルがその形でページを読み込んでいない場合、調査以前に実行の機会自体がない可能性が高い
- 「対応が難しい」というより「そもそも対象外である可能性が高い」という、より確信の持てる結論に至った

### 対応
- README.md/README.en.mdに「対応範囲(Supported / Not currently supported)」セクションを追加
- ROADMAPの将来検討事項に、調査結果とあわせて「見送り」として記録

### 次回への申し送り
- 韓国語対応のみ、将来検討事項として残っている

---

## 2026-07-22　Chrome Web Store申請完了、韓国語のお楽しみコメントを追加(v0.6.0)

### Chrome Web Store
- 開発者登録・ストア掲載情報の入力・「リモートコードを使用していますか?」への回答(いいえ、で正しいことを
  コードを再検証して確認)を経て、審査のため送信完了。結果待ちの状態に

### 表記修正
- DEVLOG内の「CODEX」を、公式表記の「Codex」に一括修正

### X(旧Twitter)アカウント開設
- 更新情報の発信用にXアカウントを開設、GitHubのプロフィール欄にリンク
- 慣れていないため、うまくいかなければ休止するかもしれないとのこと。README等への追記は
  本人の希望があってから対応する方針(今回はまだ追加していない)

### 韓国語のお楽しみコメントを追加
- ユーザーが韓国語版のコメント一式を用意(直訳ではなく、韓国語らしい表現・文化的な違いを
  踏まえて調整されたもの。日本語版・英語版と1対1では対応していない)
- `fun/comments.ko.js`を新設、`fun-engine.js`の言語選択ロジックに韓国語を追加
  (日本語→日本語、韓国語→韓国語、それ以外→英語、のフォールバック)
- manifest.jsonの読み込みファイルを更新
- Node.jsでのモックテストで、ko-KR/ja/en-US/未対応言語(fr)それぞれの判定を確認
- UIの表示文言(`_locales/ko`)はまだ未対応。お楽しみコメントのみ先行対応という形にした
  (ROADMAPにも記録)

### 次回への申し送り
- Chrome Web Storeの審査結果を待つ
- 韓国語のUI文言対応(`_locales/ko`)は今後の課題として残っている

---

## 2026-07-22　"Extension context invalidated" エラーへの対応(v0.6.1)

### 報告内容
- Claude.aiの開きっぱなしタブで `Uncaught Error: Extension context invalidated.` が発生
  (`core/content-main.js`の`chrome.i18n.getMessage`呼び出しで発生)

### 原因
- 拡張機能を`chrome://extensions`で再読み込みすると、その時点で既に開いていたタブの
  コンテンツスクリプトは「古いつながり」のまま取り残される
- その状態で`chrome.*`のAPIを呼ぶとこのエラーが発生する。開発中に何度も拡張機能を
  再読み込みしてきたため、開きっぱなしだったタブで起きたと考えられる
- 実際に公開後も、将来のアップデート配信時に同じことがユーザー環境で起こり得るため、
  対応する価値があると判断

### 対応
- `core/dom-utils.js`に`safeCall(fn, fallback)`を追加し、`chrome.i18n`・`chrome.storage`の
  呼び出しをtry/catchで安全化。エラー時は既定値に静かにフォールバックするように変更
  (`core/content-main.js`, `core/settings-store.js`, `fun/fun-engine.js`)
- MAIN worldで動く`chatgpt-page-guard.js`・`gemini-page-guard.js`は元々`chrome.*`APIを
  使用していないため、この問題の影響を受けないことを確認
- ロック中のEnter遮断など、送信ロックの安全性そのものはこの問題の影響を受けないことを確認
  (影響が出るのはバッジ表示・お楽しみ機能などの付随機能のみ)
- 根本的な解消(コンテキストの再接続)にはタブの再読み込みが必要なため、その旨をユーザーに案内

### 次回への申し送り
- 特になし。Chrome Web Storeの審査結果待ち

---

## 2026-07-22　コメントの言語を設定画面で切り替えられるように(v0.7.0)

### 報告内容
- 韓国語コメントの動作確認のため、Chromeの表示言語自体を韓国語に変更したところ、
  文字が読めなくなり元に戻すのに苦労した(ブラウザの言語設定を変えるのはリスクが高い方法だった)

### 対応
- 設定画面(ポップアップ)に「コメントの言語」というプルダウンを追加
  (自動/日本語/English/한국어)。「自動」以外を選ぶと、ブラウザの表示言語に関係なく
  お楽しみコメントの言語を固定できる
- `core/settings-store.js`に`commentLang`(既定値`auto`)を追加
- `fun-engine.js`の`showRandomComment`を、設定を確認してから表示する流れに変更
  (`commentLang`が`auto`または未設定ならこれまで通りブラウザの表示言語から自動判定)
- Node.jsのモックテストで、`auto`/`ja`/`ko`/`en`それぞれの指定が正しく反映されることを確認

### 効果
- これでユーザーは、二度とブラウザの表示言語を変える必要がなくなった
  (設定画面から選ぶだけで、安全にコメントの表示を確認できる)

### 次回への申し送り
- 特になし。Chrome Web Storeの審査結果待ち

---

## 2026-07-22　ポップアップのレイアウト修正、韓国語のUI・README対応(v0.8.0)

### 報告内容
- ポップアップの「コメントの言語」の行が、ラベルが長いためにプルダウンと詰まって
  文字が縦に折り返ってしまうレイアウト崩れが発生
- 韓国語対応の勢いで、ポップアップUIとREADMEも韓国語にしたいとの要望
- ChatGPTから、名称を「SendGuard」(スペース無し)に統一する提案があったと共有あり

### 対応
- `popup.css`に`.row-stacked`を追加し、ラベルが長い行は「ラベル→プルダウン」を
  縦に積むレイアウトに変更(プルダウンは行いっぱいの幅に)。Visualizerで簡易プレビューして確認
- `_locales/ko/messages.json`を新設。ポップアップ・バッジの表示文言を韓国語対応
  (Chromeの言語判定により、韓国語ブラウザで自動的に反映される)
- `README.ko.md`を新規作成(README.en.mdをベースに全文翻訳)
- `docs/images/how-it-works.ko.svg`(韓国語版の使い方図解)を新規作成
- README.md/README.en.mdの言語切り替えリンク・フォルダ構成説明を更新
- DEVLOG末尾に紛れ込んでいた、v0.4.1の重複エントリ(統合作業時の紛れ込みと思われる)を整理・削除

### 次回への申し送り
- 「SendGuard」(スペース無し表記)への名称統一案は、変更箇所が多いため、
  別途あらためて相談してから着手する方針
- Chrome Web Storeの審査結果待ち

---

## 2026-07-25　「Enterlude」への改名(v1.0.0)

### 経緯
- 「SendGuard」(スペース無し統一)案について商標調査をしたところ、類似ジャンルで既存の名前が
  複数見つかったため見送りに
- 別スレッドのClaudeに新しい名前のアイデア出しを依頼し、「Enterlude」(Enter + interlude=幕間)を
  採用。商標の重複がないことも確認済みとのこと
  - 由来: 送信前に挟まる「小さな幕間」というニュアンス。機能の本質(送信前の"間")を表しつつ、
    多言語で発音しやすく、ロゴ・アイコンにも展開しやすい語感
- Chrome Web Storeの審査を一度取り下げ、新名称で再申請する方針
- 変更範囲が広いため、まず変更対象の一覧化(manifest/_locales/README/ドキュメント/コード内識別子/
  画像/GitHub関連)を行ってから、段階を分けて進めることにした
- バージョンは、名称の正式なお披露目・韓国語対応も合わせた区切りとして`v1.0.0`にすることに決定

### やったこと(第1段階: 作業環境内での変更)
- コード内部の識別子を統一(ユーザーには見えない部分)
  - `window.SendGuard` → `window.Enterlude`、`window.SendGuardComments` → `window.EnterludeComments`
  - `data-send-guard-unlocked`/`-site` → `data-enterlude-unlocked`/`-site`
    (content-main.js・chatgpt-page-guard.js・gemini-page-guard.jsの3ファイルをまたぐため、
    一致していることを確認しながら慎重に置換)
  - カスタムイベント名`send-guard:state`/`:sent` → `enterlude:state`/`:sent`
  - ガードフラグ`__sendGuardChatGPTPageGuardInstalled`等も統一
  - 置換後、構文チェックと名前空間の連携動作(lockGuard/settingsStore/adapter/funEngineの読み込み)をテストして確認
- 表示名を更新
  - `_locales`(ja/en/ko)の`appName`/`popupTitle`をEnterludeベースに変更
  - `popup.html`の`<title>`タグを更新
- README(3言語)・図解(SVG)のタイトル・ロゴのalt属性を更新
- ストア掲載用プロモ画像(`store-promo-440x280.png`)を、画像内に焼き込まれていた文字も
  含めて「Enterlude」名義で作り直し
- ROADMAPの名称変更の項目を、決定事項として更新
- GitHubのURLは今回あえて変更せず維持(リポジトリ名変更が済んでいないため、今変更すると
  一時的にリンク切れになる。安全のため、リポジトリ名変更後に別コミットでまとめて更新する方針)
- 過去のCHANGELOG/DEVLOGの記録は、ユーザーの意向で書き換えず、当時の名称のまま保持

### 次回への申し送り
- 第2段階: GitHubリポジトリ名を`send-guard`→`enterlude`に変更(ユーザー側の作業)
- 第3段階: リポジトリ名変更後、README等のGitHub URLをまとめて更新する別コミットを作成
- 第4段階: Chrome Web Storeの再申請、Xプロフィールの表示名変更(いずれもユーザー側の作業)

---

## 2026-07-26　GitHubリポジトリ名変更を反映(第3段階)

### やったこと
- ユーザーがGitHub側でリポジトリ名を`send-guard`→`enterlude`に変更完了
- README(3言語)・ROADMAP・docs/store-listing.md内のGitHub URLを、新しいリポジトリ名に一括更新
  (CHANGELOG/DEVLOGにはURL記載が無いことを確認済みのため、対象外)
- ROADMAP冒頭の「改名作業中」の表記を「改名完了」に更新

### 次回への申し送り
- 第4段階: Chrome Web Storeの新名称での再申請、Xプロフィールの表示名変更(いずれもユーザー側の作業)

---

## 2026-07-26　ストア再申請素材の準備、プライバシーポリシー作成(v1.0.1)

### サブタイトルの相談
- Enterludeへの改名にあたり、ストア掲載用のサブタイトルについてChatGPTから4案の提案があり、
  ユーザーと一緒に検討
  - 案1(実用的・翻訳しやすい)/ 案2(ブランド寄り)/ 案3(対応サービス名を列挙)/ 案4(詩的)
  - 案3は「ChatGPT・Claude・Gemini」と他社商標を複数列挙する形になるため、ストアの「表示名」に
    使うのは商標面でやや慎重になった方がよいと助言(説明文の本文中で「対応サービス」として
    書く分には問題ない)
  - 結論: 案1を採用。ストアは実用的な説明、README・ロゴ・Xでブランドの世界観を出す、という
    ChatGPTの「二段構え」の考え方に合意
- `_locales`の英語・韓国語のappNameを案1のワーディングに更新。韓国語の用語も「오발송」に統一
  (fun/comments.ko.jsで既に使われていた表現に合わせた)

### プライバシーポリシー
- Chrome Web Store申請には「Privacy policy URL」の入力が必要なため、ChatGPTのアドバイスを
  踏まえてプライバシーポリシーページを新規作成
  - `docs/privacy.md`(日本語) / `.en.md` / `.ko.md`の3言語、README同様の相互リンク形式
  - 内容: データ収集なし、保存されるのは設定値(お楽しみ機能ON/OFF・言語設定)のみでローカル完結、
    外部通信・分析ツール・広告なし、無料でサブスクリプションなし、オープンソースである旨、
    GitHub Issueでの問い合わせ先
  - GitHub Pagesで公開する想定(`docs/`フォルダをソースに設定する方法を案内予定)

### ストア掲載テキストの更新
- `docs/store-listing.md`をEnterlude名義に更新
- 「広告表示・有料機能・サブスクリプションは一切ありません」の一文を追加(ユーザーからの要望)
- プライバシーポリシーURLの案内・カテゴリ・Privacy practicesタブの回答例も整理

### 次回への申し送り
- ユーザーがGitHub Pagesを有効化(`docs/`フォルダをソースに設定)
- Chrome Web Storeで新しいZIP・画像・説明文・プライバシーポリシーURLを使って再申請
- Xプロフィールの表示名変更(任意のタイミング)

---

## 2026-07-27　台湾版コメント追加、配信状況の管理を開始(v1.1.0)

### 台湾版のお楽しみコメント
- ユーザーが繁体字中国語(台湾)版のコメント一式を用意。台湾はAIチャットサービスの利用制限が
  ないため、使ってくれる人がいるかもしれない、とのこと
- `fun/comments.zh-TW.js`を新設。`fun-engine.js`の言語判定に`zh-TW`(`zh-hant`も含む)を追加
- 設定画面の言語プルダウンにも「繁體中文」を追加
- Node.jsのモックテストで、自動判定・手動指定どちらも正しく動作することを確認

### 組み込みタイミングの相談
- 「Chrome Web Storeの審査結果が出てから、ストアへの反映はまとめて行う」という方針に。
  GitHub側は先に更新しておき、ストア提出はタイミングを見て行う、という進め方に合意
- 「GitHubとストアのバージョンがズレる」ことについて質問があり、これは一般的なソフトウェア配布で
  普通に起きることだと説明。Gitタグで「どの時点をどこに出したか」を管理する方法を提案し、
  ROADMAPに「配信状況」の表を新設(GitHub/Chrome Web Store/Edgeそれぞれの状態を記録)

### 次回への申し送り
- Chrome Web Storeの審査結果を待つ。結果が出たら、指摘対応版(+zh-TW対応)をまとめて
  Chromeの更新 → Edgeへの新規申請、の順で進める
- 未着手の要望: 印刷時にロックバッジを非表示にする、ポップアップに閉じる(×)ボタンを実装する

---

## 2026-07-29　印刷時のバッジ非表示、ポップアップの閉じるボタン実装(v1.2.0)

### 新しいスレッドへの引き継ぎ
- 別スレッドから初めて合流。GitHub(Public)のクローンと、ユーザー提供のgit bundleの両方から
  状況を確認。DEVLOG/CHANGELOG/ROADMAP/READMEを読んで経緯を把握してから作業開始

### 実装したこと
- `core/lock-ui.css`に`@media print`ルールを追加し、印刷時に`.sg-badge`(ロックバッジ)と
  `.sg-toast`(お楽しみコメントのトースト)を非表示にした
- ポップアップ(`popup/`)右上に閉じる(×)ボタンを追加
  - `popup.html`にボタン要素を追加、`popup.css`でスタイリング(ホバー・フォーカスリング含む)
  - `popup.js`で`window.close()`を紐付け。同時に、aria-label用の新しいi18nパターン
    (`data-i18n-aria`属性)を追加し、既存の`data-i18n`(textContent用)と使い分けられるようにした
  - `_locales`のja/en/koに`closeButtonAria`キーを追加
  - 実装前にVisualizerでプレビューを提示し、了承を得てから着手(ユーザーとの約束通り)

### ついでに見つけた不具合の修正
- `core/lock-ui.css`を見ていて、Enterludeへの改名時(`data-send-guard-unlocked`等を
  `data-enterlude-*`に統一した際)に、狭い画面幅×Gemini利用時のバッジ位置調整用セレクタ
  `data-send-guard-site`だけ取り残されているのを発見。`content-main.js`側は既に
  `data-enterlude-site`を設定しているため、このCSSルールは実質死んでいた
  (実害は軽微: 狭い画面でGeminiを使う時、バッジが送信ボタンと重なる可能性があった程度)
- `data-enterlude-site`に修正。ユーザーに発見経緯を報告済み

### 動作確認
- Node.js + jsdomでモックテストを実施(popup.htmlをロードし、chrome.i18n/chrome.storageを
  モック化)。閉じるボタンの存在・aria-label反映・クリック時の`window.close()`呼び出し、
  既存のh1タイトルi18n(リグレッション確認)、`lock-ui.css`の`@media print`ブロックの中身、
  および上記の不具合修正箇所を含め、計10項目すべてPASSを確認

### バージョン管理
- 後方互換性のある機能追加のため、MINORバージョンアップ: v1.1.0 → v1.2.0
- `manifest.json`のバージョンを更新、CHANGELOGに追記

### 次回への申し送り
- Chrome Web Storeの審査結果はまだ待ち。結果が出たら、指摘対応版(+zh-TW対応 + 今回のv1.2.0の
  変更)をまとめてChromeの更新 → Edgeへの新規申請、の順で進める予定は変わらず
- 今回の変更はGitHubのみ反映。ストアへの反映タイミングは引き続き審査結果待ち

---

## 2026-07-29　別スレッドのコードレビュー反映(v1.2.1)

### 経緯
- ユーザーが別スレッドのClaudeにコードレビューを依頼し、結果を🔴🟡🟢の3段階でまとめて共有。
  🔴の3件のうち、3番目(Claude.aiの入力欄判定`div[contenteditable="true"]`が広すぎる懸念)は
  ユーザーが実機確認済みで、メッセージ編集画面でも改行にならず確定できることを確認。
  今回は🔴の1・2番目を対応

### 修正したこと(実コードで再現・確認した上で対応)
- **🔴1: ChatGPTの「再生成」ボタンがロック中でも押せてしまう不具合**
  - `adapters/chatgpt.js`(表向きの定義)と`core/chatgpt-page-guard.js`(実際に動く方)の
    RETRY_SELECTORを見比べ、指摘通り後者に「再生成」ラベルが抜けていることを確認
  - `core/chatgpt-page-guard.js`に追加して解消。念のため`core/gemini-page-guard.js`と
    `adapters/gemini.js`も同様のズレがないか比較したが、こちらは完全に一致しており問題なし
- **🔴2: Claude.aiで日本語入力の変換確定Enterが誤って改行扱いになる可能性**
  - `core/content-main.js`のkeydownハンドラに`isComposing`チェックが欠けていることを確認
    (ChatGPT/Gemini専用ガードには既にあった)。同じ書き方で1行追加して解消

### 動作確認
- Node.js + jsdomでモックテストを新規作成(`test-fixes.js`)。以下を確認:
  - ChatGPT: 「再生成」ボタンがロック中にクリックされた際、preventDefaultされることを確認
  - ChatGPT: 既存の"Regenerate"ラベルも引き続き機能すること(リグレッション確認)
  - Claude.ai: `isComposing: true`および`keyCode: 229`のEnterが、preventDefaultされず
    execCommandも呼ばれないこと(=サイト本来のIME確定動作を妨げない)
  - Claude.ai: 通常のEnter(変換中でない)は、ロック中なら引き続きブロックされ改行が
    挿入されること(リグレッション確認)
  - 念のため、修正前のコード(直前のコミット)に対して同じテストを実行し、修正前は
    アサーションが成立しない(=バグを検知できる)ことも確認した上で、修正後は全7項目PASS

### バージョン管理
- 後方互換性のあるバグ修正のため、PATCHバージョンアップ: v1.2.0 → v1.2.1

### 次回への申し送り
- 🟡・🟢に分類された残りの指摘(page-guard.jsとadapters/*.jsの重複コード整理、
  popup.jsの設定読み書きをsettings-store.js経由に統一、execCommandの非推奨対応、
  zh-TWのUI文言未対応など)は、優先度が高くないため今回は未対応。今後どこかで
  まとめて着手するか、都度ユーザーと相談して進める
- Chrome Web Storeの審査結果待ちは変わらず

---

## 2026-07-29　コードレビュー指摘🟡4番: セレクタ定義の一本化(v1.2.2)

### 経緯
- 前回のレビュー指摘🟡4番(ChatGPT・Gemini用の判定ロジックが死んだコードになっている件)に対応。
  ユーザーに「コメントで明記するだけ」と「セレクタを1箇所にまとめる」の2案を提示して相談し、
  再発防止を優先して後者を選んでいただいた

### 実装したこと
- `core/chatgpt-selectors.js`・`core/gemini-selectors.js`を新設し、
  COMPOSER_SELECTOR / SEND_SELECTOR / RETRY_SELECTORをサイトごとに1箇所へ集約
  - 内容は、実際に動いている`core/*-page-guard.js`側の(実機で確認済みの)定義を正とした
  - MAIN worldとISOLATED worldは別々のJS実行環境なので変数そのものは共有できないが、
    同じファイルを両方のworldに読み込ませることで「直す場所は1箇所」を実現している
- `manifest.json`の該当4箇所(MAIN world×2、ISOLATED world×2)に、新設ファイルを
  対応するpage-guard/adapterより前の順番で追加
- `adapters/chatgpt.js`・`adapters/gemini.js`は、共有セレクタを参照する形に変更した上で、
  「ChatGPT/Geminiでは実際には呼ばれない(死んだコード)」旨と、その理由・残している意図を
  コメントで明記した

### 動作確認
- Node.js + jsdomで新規モックテスト(`test-consolidation.js`、21項目)を実施:
  - `manifest.json`で、セレクタファイルがpage-guard/adapterより前に読み込まれる順番に
    なっていることを確認
  - MAIN world側(page-guard.js)・ISOLATED world側(adapters/*.js)それぞれで、
    セレクタファイルと組み合わせてエラーなく動作し、ボタン判定が引き続き正しいこと
    (リグレッション確認)
  - 4ファイルすべてで、セレクタ配列のハードコードが残っていない(参照のみになった)ことを確認
- 前回作成した`test-fixes.js`・`test-popup.js`も再実行し、既存の28項目すべてPASSを再確認
  (test-fixes.jsは、production側のスクリプト読み込み順に合わせてセレクタファイルの
  読み込みを追加する必要があったため、テスト側も更新した)
- 計49項目すべてPASS

### バージョン管理
- 挙動を変えないリファクタリングだが、コードの再発防止という実質的な改善のため
  PATCHバージョンアップ: v1.2.1 → v1.2.2

### 次回への申し送り
- 残りの🟡・🟢(popup.jsの設定読み書き統一、execCommand非推奨対応、zh-TWのUI文言未対応など)は
  引き続き未対応。優先度含めユーザーと相談しながら進める
- Chrome Web Storeの審査結果待ちは変わらず

---

## 2026-07-31　開発をこのスレッドで再開、台湾版README・プライバシーポリシー追加(v1.3.0)

### 開発スレッドの引き継ぎについて
- ChatGPT/Geminiのロック機能停止(Codexが原因特定・修正)という出来事があり、ユーザーが
  「開発は元のスレッドで続けたい」との意向。DEVLOGの連続性が途切れる形になるが、
  経緯を都度読み合わせることで対応する方針に
- Chrome Web Storeの審査は2026-07-29に無事通過(v1.0.1として公開)
- 未pushのローカル状態(v1.2.3、Codexの修正込み)を`git bundle`で共有してもらい、
  全体を検証(構文・manifest整合性・旧命名の残存・セキュリティ観点・最近の機能追加)
  → 問題なしを確認

### 台湾版README・プライバシーポリシーの追加
- `README.zh-TW.md`(README.en.mdをベースに全文翻訳)を新規作成
- `docs/images/how-it-works.zh-TW.svg`(使い方の図解)を新規作成
- `docs/privacy.zh-TW.md`(プライバシーポリシー)を新規作成
- README(4言語)・プライバシーポリシー(4言語)・`docs/index.md`の言語切り替えリンクを
  すべて相互リンクするよう更新
- ついでに見つけた不具合を修正: README(ja/en/ko)のバージョンバッジが`v1.1.0`のまま
  更新されていなかった
- ROADMAPの「現在地」「配信状況」を、Chrome Web Store審査通過を反映して更新

### 今後の運用について相談したこと
- リファクタリングなど壊れるリスクのある変更は、いきなり`main`にコミットせず、
  作業用のブランチ(`work/xxx`)を切ってから試し、動作確認できたものだけ`main`に
  マージする運用を提案。次回、実際にブランチを使う場面で具体的に導入する予定
- MAIN world関連のファイルを変更する際は、「正常な読み込み順」だけでなく
  「セレクタ定義が読み込まれる前に動かした場合」のような意地悪なテストも
  行うことの重要性を確認(今回のバグはまさにこのケースで発生していた)

### 次回への申し送り
- ここまでの内容(台湾版README/図解/プライバシーポリシー)をバンドルで共有し、
  ユーザー側でpull → push
- その後: 動作確認 → Chrome Web Store(2回目の申請、指摘対応版) → Edge Add-ons(初回申請)
  の順で進める予定
- 残りの🟡・🟢(popup.jsの設定読み書き統一、execCommand非推奨対応、zh-TWのUI文言未対応など)は
  引き続き未対応

---

## 2026-07-31　繁体字中国語のUI文言対応、全体の整合性チェック(v1.4.0)

### UI文言(zh-TW)の追加
- `_locales/zh-TW/messages.json`を新規作成。既存の英語版をベースに全16キーを翻訳
- ja/en/ko/zh-TWの4言語間でキーの過不足がないことをスクリプトで確認(全て一致)
- `chrome.i18n.getMessage`相当の呼び出しをNode.jsで模擬し、実際に文言が正しく
  取得できることを確認

### 全体の整合性チェック
- ユーザーからの依頼で、フォルダ構成の説明など全体を再点検
- README(4言語)の「_locales」フォルダ説明が「日本語・英語・韓国語」止まりで
  古くなっていたのを発見・修正(繁体字中国語まで含める形に)
- `docs/store-listing.md`に繁体字中国語版のストア名候補を追加(将来の多言語掲載用)
- `popup.html`の言語プルダウンには既にzh-TWが追加済みであることを確認(変更不要)
- `manifest.json`は`_locales`フォルダの追加だけで自動認識されるため、変更不要であることを確認

### 現状
- 繁体字中国語(台湾)は、お楽しみコメント・README・プライバシーポリシー・UI文言の
  すべてに対応完了。韓国語と同じ完成度に到達

### 次回への申し送り
- 動作確認後、Chrome Web Store(2回目の申請)・Edge Add-ons(初回申請)の準備に進む

---

## 2026-07-31　繁体字中国語UIが反映されない不具合を修正(v1.4.1)

### 報告内容
- Chromeの表示言語を「中文(繁體)」にして再起動しても、ポップアップや拡張機能の説明文が
  日本語のままだった。お楽しみコメントは正しく繁体字中国語で表示されていた

### 原因
- Web検索で確認したところ、Chrome拡張機能の`_locales`フォルダ名は、`chrome.i18n.getUILanguage()`
  が返すBCP-47形式(`zh-TW`、ハイフン区切り)とは別に、独自の命名規則(`zh_TW`、アンダースコア区切り)を
  使う決まりになっていた
- `fun/comments.zh-TW.js`(お楽しみコメント)は自前の言語判定ロジックで`chrome.i18n.getUILanguage()`の
  戻り値をそのまま使っていたため問題なく動いていたが、`_locales/zh-TW`フォルダの方はChrome自体が
  認識できず、`default_locale`(日本語)にフォールバックしていたと判明

### 対応
- `_locales/zh-TW` → `_locales/zh_TW`にリネーム
- コード側(`fun/comments.zh-TW.js`のファイル名、設定画面の言語選択の値`zh-TW`)は、
  Chromeの`_locales`規則とは無関係な部分のため変更不要と確認

### 教訓
- Chrome拡張機能の中でも、「JSのAPIが返す言語コード」と「`_locales`フォルダの命名規則」は
  別のルールに従っている、という点は今後もハマりやすいポイントとして記録しておく

### 次回への申し送り
- 動作確認後、Chrome Web Store(2回目の申請)・Edge Add-onsの準備に進む

---

## 2026-07-31　READMEの「対応」と「動作確認環境」を分離(v1.4.2)

### 経緯
- ユーザーが自身の拡張機能のアナリティクスでLinuxからのダウンロードを確認。実際には
  Windows 11 Homeでしか動作確認していないため、READMEの書き方についてChatGPTに相談していた
- ChatGPTの指摘: 「Chrome・Edgeの通常/シークレットモードで動作確認済み」という書き方は、
  「対応サービス・ブラウザ」の話と「実際にテストした環境」の話が混ざっており、読み手に
  誤解を与えうる。分けて書くべき、との提案
- 提案内容を検討し、妥当と判断して採用を決定

### 対応
- README(4言語: ja/en/ko/zh-TW)の「現在のステータス」を、
  「対応サービス／対応ブラウザ／動作確認環境(Windows 11 Home)」の3セクションに分割
- 「上記以外のOS・環境では、開発者による動作確認は行っていません」を明記
- Limitationsとの重複は避け、動作確認環境の情報は「現在のステータス」側にのみ記載

### 所感
- Limitationsの「完全防止を保証しない」「AIサービス側の更新で一時的に動かなくなることがある」
  という既存の記述と一貫性のある、誠実な情報開示の一つとして自然に収まった

### 次回への申し送り
- 動作確認後、Chrome Web Store(2回目の申請)・Edge Add-onsの準備に進む
